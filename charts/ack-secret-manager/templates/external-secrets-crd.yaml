---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (devel)
  name: externalsecrets.alibabacloud.com
spec:
  group: alibabacloud.com
  names:
    kind: ExternalSecret
    listKind: ExternalSecretList
    plural: externalsecrets
    singular: externalsecret
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ExternalSecret is the Schema for the externalsecrets API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ExternalSecretSpec defines the desired state of ExternalSecret
            properties:
              data:
                items:
                  properties:
                    jmesPath:
                      description: Optional array to specify what json key value pairs
                        to extract from a secret and mount as individual secrets
                      items:
                        description: An individual json key value pair to mount
                        properties:
                          objectAlias:
                            description: File name in which to store the secret in.
                            type: string
                          path:
                            description: JMES path to use for retrieval
                            type: string
                        required:
                        - objectAlias
                        - path
                        type: object
                      type: array
                    key:
                      type: string
                    kmsEndpoint:
                      type: string
                    name:
                      type: string
                    secretStoreRef:
                      properties:
                        kind:
                          description: |-
                            Kind of the SecretStore resource (SecretStore or ClusterSecretStore)
                            Defaults to SecretStore
                          enum:
                          - SecretStore
                          - ClusterSecretStore
                          type: string
                        name:
                          type: string
                        namespace:
                          description: 'Deprecated : Namespace field is deprecated,
                            use SecretStore in the same namespace as ExternalSecret,
                            or use ClusterSecretStore'
                          type: string
                      required:
                      - name
                      type: object
                    versionId:
                      type: string
                    versionStage:
                      type: string
                  required:
                  - key
                  type: object
                type: array
              dataProcess:
                items:
                  properties:
                    extract:
                      properties:
                        jmesPath:
                          description: Optional array to specify what json key value
                            pairs to extract from a secret and mount as individual
                            secrets
                          items:
                            description: An individual json key value pair to mount
                            properties:
                              objectAlias:
                                description: File name in which to store the secret
                                  in.
                                type: string
                              path:
                                description: JMES path to use for retrieval
                                type: string
                            required:
                            - objectAlias
                            - path
                            type: object
                          type: array
                        key:
                          type: string
                        kmsEndpoint:
                          type: string
                        name:
                          type: string
                        secretStoreRef:
                          properties:
                            kind:
                              description: |-
                                Kind of the SecretStore resource (SecretStore or ClusterSecretStore)
                                Defaults to SecretStore
                              enum:
                              - SecretStore
                              - ClusterSecretStore
                              type: string
                            name:
                              type: string
                            namespace:
                              description: 'Deprecated : Namespace field is deprecated,
                                use SecretStore in the same namespace as ExternalSecret,
                                or use ClusterSecretStore'
                              type: string
                          required:
                          - name
                          type: object
                        versionId:
                          type: string
                        versionStage:
                          type: string
                      required:
                      - key
                      type: object
                    replaceRule:
                      items:
                        properties:
                          source:
                            type: string
                          target:
                            type: string
                        required:
                        - source
                        - target
                        type: object
                      type: array
                  type: object
                type: array
              provider:
                type: string
              rotationInterval:
                description: The time in which the controller should reconcile its
                  objects and recheck namespaces for labels.
                type: string
              target:
                description: Target defines how the secret is created in the cluster
                properties:
                  name:
                    description: |-
                      Name defines the name of the secret resource to be managed.
                      If not set, the name will be auto-generated based on the ExternalSecret name.
                    type: string
                  template:
                    description: Template defines a template that can be used to generate
                      or transform the secret data
                    properties:
                      data:
                        additionalProperties:
                          type: string
                        description: Data defines the target secret's data field.
                        type: object
                      mergePolicy:
                        default: Replace
                        description: |-
                          MergePolicy defines how template results should be merged with the original data.
                          Defaults to "Replace"
                        enum:
                        - Replace
                        - Merge
                        type: string
                      metadata:
                        description: Metadata defines the target secret's metadata
                          fields.
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: Annotations to apply to the target secret.
                            type: object
                          labels:
                            additionalProperties:
                              type: string
                            description: Labels to apply to the target secret.
                            type: object
                        type: object
                      templateFrom:
                        description: TemplateFrom specifies sources for templates.
                        items:
                          description: |-
                            TemplateFrom specifies a source for templates.
                            Each item in the list can either reference a ConfigMap or a Secret resource.
                          properties:
                            configMap:
                              description: TemplateRef specifies a reference to either
                                a ConfigMap or a Secret resource.
                              properties:
                                items:
                                  description: A list of keys in the ConfigMap/Secret
                                    to use as templates for Secret data
                                  items:
                                    description: TemplateRefItem specifies a key in
                                      the ConfigMap/Secret to use as a template for
                                      Secret data.
                                    properties:
                                      key:
                                        description: A key in the ConfigMap/Secret
                                        maxLength: 253
                                        minLength: 1
                                        pattern: ^[-._a-zA-Z0-9]+$
                                        type: string
                                      templateAs:
                                        default: Values
                                        description: TemplateScope specifies how the
                                          template keys should be interpreted.
                                        enum:
                                        - Values
                                        - KeysAndValues
                                        type: string
                                    required:
                                    - key
                                    type: object
                                  type: array
                                name:
                                  description: The name of the ConfigMap/Secret resource
                                  maxLength: 253
                                  minLength: 1
                                  pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                                  type: string
                              required:
                              - items
                              - name
                              type: object
                            literal:
                              type: string
                            secret:
                              description: TemplateRef specifies a reference to either
                                a ConfigMap or a Secret resource.
                              properties:
                                items:
                                  description: A list of keys in the ConfigMap/Secret
                                    to use as templates for Secret data
                                  items:
                                    description: TemplateRefItem specifies a key in
                                      the ConfigMap/Secret to use as a template for
                                      Secret data.
                                    properties:
                                      key:
                                        description: A key in the ConfigMap/Secret
                                        maxLength: 253
                                        minLength: 1
                                        pattern: ^[-._a-zA-Z0-9]+$
                                        type: string
                                      templateAs:
                                        default: Values
                                        description: TemplateScope specifies how the
                                          template keys should be interpreted.
                                        enum:
                                        - Values
                                        - KeysAndValues
                                        type: string
                                    required:
                                    - key
                                    type: object
                                  type: array
                                name:
                                  description: The name of the ConfigMap/Secret resource
                                  maxLength: 253
                                  minLength: 1
                                  pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                                  type: string
                              required:
                              - items
                              - name
                              type: object
                            target:
                              default: Data
                              description: |-
                                Target specifies where to place the template result.
                                For Secret resources, common values are: "Data", "Annotations", "Labels".
                              enum:
                              - Data
                              - Annotations
                              - Labels
                              type: string
                          type: object
                        type: array
                      type:
                        description: Type defines the target secret's type field.
                        type: string
                    type: object
                type: object
              type:
                type: string
            type: object
          status:
            description: ExternalSecretStatus defines the observed state of ExternalSecret
            properties:
              dataSyncResults:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                items:
                  properties:
                    ExternalSecretKey:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    synchronizationTime:
                      format: date-time
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
